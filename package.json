{
  "name": "annotation-app-v2",
  "version": "1.0.0",
  "private": true,
  "description": "Local stepwise advertisement face annotation app.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "start:expert": "node server.js --expert",
    "dev:hosted": "node scripts/dev-hosted.js",
    "test": "node --test",
    "db:migrate": "node scripts/migrate.js",
    "auth:hash": "node scripts/hash-password.js",
    "backup:exports": "node scripts/backup-exports.js",
    "validate:manifest": "node scripts/validate-manifest.js",
    "upload:set": "node scripts/upload-set.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1090.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.14"
  }
}
